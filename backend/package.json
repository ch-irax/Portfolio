{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Clean and simple Node.js backend for personal portfolio",
  "type": "module",
  "main": "server.js",
 "scripts": {
  "start": "node --env-file=.env server.js",
  "dev": "node --env-file=.env --watch server.js",
  "build": "echo 'No build needed for Express API'"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "mongoose": "^8.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
