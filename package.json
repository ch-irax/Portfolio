{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Clean and simple Node.js backend for personal portfolio",
  "type": "module",
  "main": "server.js",
  "scripts": {
  "start": "node --env-file=.env server.js",
  "dev": "node --env-file=.env --watch server.js"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "nodemailer": "^8.0.10",
    "sqlite3": "^5.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
